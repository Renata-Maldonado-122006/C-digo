#include <stdio.h>

int main (){
  int HST =30;
  if PH =5;
    printf("Si las horas semanales trabajadas es menor o igual a 40");
    if("HST*40");
    scanf("%d", &HST);
    printf("Si las horas semanales trabajadas es mayor a 40");
    if(40*HP + (PH*1.5*(HST-40)));
    scanf("%d", &HP);
    salario=(40 * PH) + (PH * 1.5 * (HST - 40));
    printf("El salario semanal es:%0.5 f\n", salario);
    return 0;
  }
  